# Decision Log — Append Only

| ID | Date | Decision | Source | Consequence |
|---|---|---|---|---|
| D-001 | 2026-08-13 | Use a turn-based narrative investigation micro-RPG | user interview round 1 | real-time combat is excluded |
| D-002 | 2026-08-13 | Deliver planning plus a headless experimental slice | user interview round 2 | polished desktop release is not implied |
| D-003 | 2026-08-13 | Use Godot 4.x headless-first | user interview round 3 | Unity-specific guidance is out of scope |
| D-004 | 2026-08-13 | Use structured-state primary and frozen-image VLM secondary tracks | user interview round 4 | image generation is offline only |
| D-005 | 2026-08-13 | Design human tooling/protocol but collect no participant data | user interview round 5 | efficacy and ethics approval remain future gates |
| D-006 | 2026-08-13 | Apply a zero-economy research override | director, derived from noncommercial scope | no paid/free G5 claim |
| D-007 | 2026-08-13 | Preserve root research rules and use project `CLAUDE.md` as the canonical studio contract | director | no duplicated studio contract |
| D-008 | 2026-08-13 | Classify current Godot execution as an engine-local authored policy mirror | independent integration review | live Python authorization round-trip remains a paper gate |
| D-009 | 2026-08-13 | Reject SL-C04 v1 and promote regenerated canonical-visible-only v2 | independent visual audit | rejected bytes and reason remain preserved |
| D-010 | 2026-08-13 | Validate save checksums before mutating live engine state | independent safety review | corrupt-save fixture becomes mandatory regression evidence |
| D-011 | 2026-08-13 | Separate logical event sequence from physical JSONL delivery order | independent contract review | event schema 1.1 adds contiguous `delivery_index`; duplicate delivery reuses logical sequence |
| D-012 | 2026-08-13 | Re-derive the studio rule after authority and event-contract changes | studio refresh rule | `CLAUDE.md` names the current policy mirror and blocks live-integration claims |
| D-013 | 2026-08-13 | Promote each Godot capture under a unique evidence-set ID | immutable evidence contract | capture in staging, reserve target without replacement, move manifest last, and fail closed on an existing ID |
| D-014 | 2026-08-13 | Close Cycle 1 independent review as bounded approval | game-integrator + logic-auditor read-only recheck | design, concepts, engine-local fixtures, and evidence retention approved; live Python transport, G6, and efficacy remain `FIX` |
| D-015 | 2026-08-13 | Open Cycle 2 as a bounded non-headless render-capture evidence pass | director from approved next beat | retain the headless conformance lane and add a separate deterministic presentation lane; no scope expansion to a playable release |
| D-016 | 2026-08-13 | Classify paper bundle `SL-CAPTURE-001` as authored fixture render/state correspondence only | director + paper integration review | manifest captures `sl-rc-001-arrival`, `sl-rc-002-rejected-secret`, and `sl-rc-003-authorized-hint` may support `C-GAME-DESIGN-003`; they cannot support efficacy, usability, immersion, G4, G6, or live Python claims |
| D-017 | 2026-08-13 | Exclude generated concept art from the primary render-capture pass | structured-state primary-track boundary | primary PNGs contain only deterministic state/text and programmatic engine graphics; generated assets remain secondary-track only |
| D-018 | 2026-08-13 | Require non-headless render metadata and source-state binding before screenshot promotion | evidence-integrity review | a capture must bind its source fixture/run/event or state beat, hashes, display/render method, dimensions, bytes, SHA-256, and non-blank checks; a headless substitute fails closed |
| D-019 | 2026-08-13 | Re-derive the studio rule for the separate render-capture entry point and claim boundary | studio refresh rule | `CLAUDE.md`, manifest, QA, ops, resource, and bilingual public documentation must stay synchronized with the selected immutable evidence set |
| D-020 | 2026-08-13 | Retain render v1 but select render v2 after visual QA found a clipped rejection header | visual inspection + immutable evidence rule | `godot-4.7.1-20260813t114443z-sealed-lighthouse-render-v1` remains immutable and superseded; `godot-4.7.1-20260813t114702z-sealed-lighthouse-render-v2` is the current paper-facing packet |
| D-021 | 2026-08-13 | Close bounded Cycle 2 render-capture work after fresh aggregate and visual verification | `./scripts/validate_game_track.sh` + three-panel visual inspection | 14 tests and 33 subtests pass; `SL-CAPTURE-001` is paper-citable only for authored render/state correspondence; next cycle re-enters Stage 1 for live Python transport and performance evidence |
| D-022 | 2026-08-13 | Reopen the bounded close for independent visual polish and select render v3 | independent three-panel visual audit + immutable evidence rule | v1 and v2 remain retained superseded; `godot-4.7.1-20260813t114945z-sealed-lighthouse-render-v3` is current because it preserves safe top margins and high-contrast state status without changing the authored fixture or claim scope |
| D-023 | 2026-08-13 | Re-close bounded Cycle 2 against selected v3 and enforce current-pointer parity in public docs | fresh aggregate regression + independent visual audit | 16 tests and 39 subtests pass; six public game documents resolve the current v3 ID; live Python transport, G4, G6, and all efficacy claims remain `FIX` |
| D-024 | 2026-08-13 | Confirm the v3 close after authoritative bilingual paper/PDF integration | fresh game-track and full-suite regressions after paper rebuild | game track passes 17 tests/39 subtests and full suite passes 80 tests/42 subtests; both PDFs remain six pages; bounded claim scope is unchanged |
| D-025 | 2026-08-13 | Retain render v1--v3 and select render v4 after deep provenance audit | independent evidence-chain audit | v4 binds hashes for the capture pipeline, PNG decoder, capture schema, retained validator, and `uv.lock`, plus Python/jsonschema versions; image bytes and bounded claim scope remain unchanged |
| D-026 | 2026-08-13 | Retain render v1--v4 and select render v5 after CI-portability repair | retained-validator portability review | `godot-4.7.1-20260813t115916z-sealed-lighthouse-render-v5` is current; verification preserves recorded capture-toolchain provenance without requiring the verifier host's Python version to equal the capture host |
| D-027 | 2026-08-13 | Re-enter Stage 1 for Cycle 3 as a public-safe playable presentation and Web release engineering pass | user request + director scope review | the current cycle may enrich presentation and deploy a procedural artifact, but selected v5 research evidence and hard-policy authority do not change |
| D-028 | 2026-08-13 | Preserve canonical `project.godot` and select `main_3d.tscn` only in a disposable Web staging copy | immutable evidence binding + release design | Web export cannot drift the project hash bound to selected v5; the ignored build artifact is non-authoritative |
| D-029 | 2026-08-13 | Exclude all pending-review generated candidate images from `web` and `--public-safe` runs | rights/style review and track-separation contract | the public artifact uses procedural geometry, VFX, UI, and audio; candidate packs remain secondary and `runtime_eligible: false` |
| D-030 | 2026-08-13 | Define the playable payoff as restoring the harbor-side signal and earning the tide route while the offshore lighthouse remains sealed | worldview audit of the prior concept promise and SL-C01 | runtime copy and future assets cannot imply that the lighthouse is entered, reopened, or lit in this slice; contradictory SL-C01 is excluded |
| D-031 | 2026-08-13 | Keep movement, focus, responsive UI, VFX, and procedural audio outside canonical state and gesture-gate browser pointer/audio activation | engine authority review + browser platform constraint | these systems may acknowledge committed state and emit intent, but only the proposal router and hard validator can authorize mutation |
| D-032 | 2026-08-13 | Classify public-safe smoke, `--evaluate`, working screenshots, and browser checks as engineering conformance only | claim-boundary review | no automated presentation artifact can support immersion, usability, affect, efficacy, G4, or G6 without the required independent measurements |
| D-033 | 2026-08-13 | Re-derive the studio contract for the playable/Web entry points and public-safe asset boundary | studio freshness rule | `CLAUDE.md`, task manifest, engineering manifests, QA gates, and retrospective checkpoint stay synchronized; G4/G6 remain `FIX` |
| D-034 | 2026-08-21 | Adopt `higgsfield-cli` (Higgsfield AI) as the generator for playable-slice UI art resources | user directive, 2026-08-21 session | `god-tibo-imagen` remains the owner for offline concept exploration; runtime-facing UI art (key art, portrait, panel textures, icons, tutorial vignettes) is generated via Higgsfield with the same provenance discipline; still never a runtime dependency |
| D-035 | 2026-08-21 | Open a curated UI asset lane `game-track/godot/assets/ui/` that is runtime-eligible for Web/public-safe builds | user directive to generate and apply UI resources; user curation stands as the human review for this lane | each shipped PNG needs adjacent provenance plus a `curation.json` naming the user directive; AI-use disclosure appears in public docs; the procedural surface must remain fully playable when the PNGs are absent; pending-review packs under `assets/concepts/` remain excluded (D-029 unchanged) |
| D-036 | 2026-08-21 | Extend the curated D-035 lane to world-surface textures and ledger stamp/seal iconography (`ui-tex-*`, `ui-stamp-*`, `ui-seal-*` under `game-track/godot/assets/ui/`) | user directive to focus 연출 (vfx, motion, 3d resource, ui, texture) on the experimental core loop | same provenance + `curation.json` + AI-disclosure + procedural-fallback contract as D-035; textures modulate existing materials only and never carry gameplay semantics; smoke receipts must be identical with the files absent |
| D-037 | 2026-08-28 | Extend the playable presentation contract to nine checks: an engine-local input-feedback telemetry probe (`input_to_visible_feedback_ms`, proposal→ledger→rendered-frame boundary) and a playfield-preservation guard with the wide ledger anchor moved 0.58 → 0.66 | user directive to deepen UI/layout/play + the frozen-but-unemitted telemetry field in `configs/experimental-game.yaml` | `--evaluate` emits `input_feedback` samples and `SL-PLAY-EVAL-001` becomes `49/49`; headless samples are wiring evidence only — browser/user-gesture latency, G4, and G6 remain `FIX` |
| D-038 | 2026-08-28 | Adopt the deterministic archetype balance probe `SL-BALANCE-PROBE-001` (five scripted rotations over the canonical machine, layout/affordance single-owner `golden_path_layout.gd`) as the G2/G3 engineering-evidence lane | user directive for game-theory/balance/fun experiments | scripted counts, G2 replacement measurements (5/5 reachability, 0/3 forbidden commits, 10/10 refusal isolation, 5/5 replay), two structurally unexercisable codes documented, duplicate-recommit machine property recorded; human balance/viability/fun gates remain `FIX` |
| D-039 | 2026-08-28 | Open the motion/rig intake lane `game-track/assets/motion/` with a raw-source redistribution guard (Mixamo FBX/DAE/BVH never git-tracked), a scratch-copy-only Blender/Blender-MCP boundary, and provenance-gated GLB staging defaulting `runtime_eligible: false` | user directive for a Mixamo→Blender→Godot retarget pipeline + Adobe raw-file redistribution ban | `validate_motion_assets.py` runs fail-closed inside the game-track regression; runtime promotion requires D-035-style curation; adopting skeletal animation in the slice remains an open interview item |
| D-040 | 2026-08-28 | Approve the RQ2 live pilot (guided ρ vs blind retry over the frozen 12-fixture battery via a CodexProposalAdapter; promotion ceiling `pilot-only`) | user interview: "승인, 바로 진행" | design frozen in `research/directions/rq2-live-pilot-plan.md`; quota use approved; frozen packet stays byte-immutable |
| D-041 | 2026-08-28 | Adopt skeletal animation through the D-039 lane, player character first, presentation-only (Mixamo manual download by the owner; retarget via BoneMap) | user interview: "도입 (플레이어 먼저)" | rig assets never mutate canonical state; procedural fallback must remain playable; first asset requires provenance + curation |
| D-042 | 2026-08-28 | Expand Higgsfield generation actively under the D-034/D-035 discipline (frozen prompts, provenance, curation, AI disclosure, procedural fallback) | user interview: "적극 확장" | commercial-rights caveat stands (terms pages 404); candidates stay `runtime_eligible: false` until curated |
| D-043 | 2026-08-28 | Scope the D-042 Higgsfield expansion to evidence-linked gaps only, and add one orientation-matched gate asset `ui-start-key-art-portrait.png` (9:16) selected on narrow layouts | audit found every asset the runtime requests already exists, so unscoped generation would have produced unused files; the measured gap was the 16:9 gate art losing ~74% of its width to STRETCH_KEEP_ASPECT_COVERED at 390x844 | one gpt_image_2 job (8.5 credits, job `50b2addf-8cf1-4e7f-bd41-6937f904f6a1`), curated under the D-035 contract with provenance + curation.json; loader falls back to landscape and then to the flat gate, and the delete-test keeps the frozen smoke hash `4b2310...8892` |
| D-044 | 2026-08-28 | Record D-041 (player rig) as blocked by two independent causes and keep the motion lane at zero assets rather than faking progress | execution attempt: Mixamo has no automation API and needs an interactive Adobe login, and Blender cannot execute Python in this sandbox (crash in `MTLBackend::metal_is_supported()` during GPU backend detection; this build accepts only `--gpu-backend metal`) | rigging stays contract-only; the blocker is recorded with its exact crash frame in `game-track/assets/motion/README.md` and must be re-adjudicated next session because `models-manifest.json` shows Blender authored the existing GLBs successfully in the past |
| D-045 | 2026-08-28 | Record that Mixamo acquisition succeeded and that straight FBX ingestion needs no Blender, correcting the D-039/D-044 assumption; keep asset bytes untracked and ship the ingestion recipe instead | user opened a Mixamo session, one rigged clip was downloaded and probed: Godot 4.7.1 imported the FBX natively (ufbx) into `Skeleton3D` 43 bones + `AnimationPlayer` `mixamo_com` + 1 `MeshInstance3D` | Blender is now optional and only needed for retargeting/mesh edits; `scripts/verify_motion_ingest.py` reproduces the check on any locally held file and writes `game-track/assets/motion/ingest-verification.json`; raw FBX stays gitignored because Adobe forbids redistributing raw character/animation files and this repo is public; runtime adoption remains a separate presentation decision |
| D-046 | 2026-08-28 | Adopt the player rig (D-041) through a local-only, untracked lane `game-track/godot/assets/rig/` that the public surface never loads | user interview approved "도입 (플레이어 먼저)", but Adobe forbids redistributing raw character/animation files and this repository is public | `load_player_rig()` refuses on Web and `--public-safe`, `build_godot_web.sh` excludes the directory from staging, and every byte except the README is gitignored; observed both ways: desktop `player_rig_active: true` playing `mixamo_com`, public-safe `false`, terminal hash `4b2310…8892` and evaluation `9/9` identical in both, so the rig is presentation-only |

Before appending, search all `_workspace/**/decision-log.md` files for the proposed ID and reread
this table tail. Never renumber history.
| D-047 | 2026-08-29 | Treat exact sandboxed macOS font/certificate diagnostics as allowed host errors in playable/balance/Web drivers while retaining a fail-closed gate for every other `ERROR`/`SCRIPT ERROR`; define balance operation coverage only over fail-closed implemented operations | generic error matching produced false negatives, while an authored-but-unimplemented operation produced a false coverage deficit | engineering-only; regression tests must prove both allowed-host and real-error directions | Keep |
| D-048 | 2026-08-29 | Recover a below-world player locally at `(0.0, 0.2, 2.0)` when `y < -3.0`, clear velocity, emit presentation feedback, and prove the symbolic hash unchanged inside the existing eighth smoke check | save/load could restore canonical state while leaving the physical avatar unrecoverable after a fall | closes the hard-lock implementation defect but not G4/G6; keep the narrow signal-lens approach open as DEF-021 | Keep |
| D-049 | 2026-08-29 | Keep the five-seed RQ2 live screening as a separate fail-closed `screening-pilot-only` pipeline, emit bilingual live fragments, promote only `C-PILOT-007/008`, and leave all `C-RESULT-*` claims `TODO-RESULT` | live receipts now exist, but they are single-model, small-cell screening evidence and must not contaminate the frozen offline denominators or imply confirmatory efficacy | 14-file manifest and per-cell signatures are verified before generation; EN/KO sources updated together; PDF rebuild still required outside this sandbox | Keep |
| D-050 | 2026-08-30 | Ship the presentation lane as English-only with ASCII-safe UI markers and replace the public procedural player body with a tracked, curated Higgsfield GLB carrying `Idle` and `Casual_Walk`; keep Mixamo raw bytes local-only and keep animation outside saves, schemas, and canonical state | user directive for English UI plus improved player asset/animation; rights and runtime audit | `assets/player/` becomes the public runtime lane with fail-closed provenance/mesh/skin/clip validation; `movement_state_changed` selects clips; Web/public-safe loads the rig; full authored smoke terminal hash remains unchanged; deployment still requires explicit approval | Keep |
| D-051 | 2026-08-30 | Add a separate simulation-only typed methods-graph lane: OKF reference export + reviewed relation overlay + closed application ontology + generated SQLite mirror + fixed-budget link-proposal keep/discard evaluation | user directive for KG/ontology improvement, precise metrics, autoresearch-style simulation, visualization, and paper/README synchronization; graph/ontology/claim-boundary audits | `SL-KG-ONTOLOGY-SIM-001` may report authored closed-world link recovery only; Graphify stays a navigation graph, Python `WorldState`/validator stay authoritative, runtime KG retrieval remains unimplemented, SQLite stays ignored, and all `C-RESULT-*` claims remain excluded | Keep |
| D-052 | 2026-08-30 | Correct D-042's now-stale 404 caveat: Higgsfield's Terms of Use (updated 2026-07-26; section 4.4) and official ownership help page state that Higgsfield does not claim outputs or restrict their commercial use | official terms and help pages verified 2026-08-29T22:47:13Z during the Task-16 release review | the user still owns input/third-party-rights review; outputs are non-exclusive and no IP indemnity is claimed; player provenance carries the frozen URL/section receipt | Keep |
| D-053 | 2026-08-30 | Deploy the English tracked-player public-safe artifact to the existing Vercel production project and record exact runtime-byte receipts without upgrading G4/G6 | user directive to complete Ponytail review/improvement through Vercel deployment; verified project-scoped REST deployment after dashboard drop and CLI paths failed | deployment `dpl_2mcMB3qomEKPyUj2oBtXVLzLXraN` is READY at `https://sealed-lighthouse-trace-rpg.vercel.app`; 11 manifest files / 50,745,203 bytes, PCK 10,892,428 bytes / `de670404…31ba`; all 10 runtime files are 200 and byte-identical, `vercel.json` is non-public configuration, desktop start → in-game → Field Guide has zero console/page errors; production save/reload, current mobile, pointer/audio, performance, soak, and rollback remain open | Keep |
| D-054 | 2026-08-30 | Rebuild and redeploy the current Godot Web artifact, replace the stale gameplay capture with a Compresso-compressed full-route MP4, and synchronize the bilingual README receipts without upgrading G4/G6 | user directive to align the paper and README with current state, deploy the playable, re-record the current version, compress it, and link both | deployment `dpl_GpRiuFSFGPrbbVMmFsPdPq731f9Y` is READY at `https://sealed-lighthouse-trace-rpg.vercel.app`; 11 manifest files / 50,745,187 bytes, 10 public runtime files / 50,744,674 bytes, PCK 10,892,412 bytes / `29e3d8b6…8f76`; all runtime files are 200 and byte-identical; production start → in-game → Field Guide logged zero console/page errors; `trace-rpg-gameplay.mp4` is H.264 High, 1280×720, 30 fps, 69.067 s, 5,662,128 bytes / `aa374c5a…7044`; production save/reload, current mobile, pointer/audio, warmed performance, soak, and rollback remain open | Keep |

| D-055 | 2026-08-30 | Make the paper contribution-first with machine-checked C1–C5/reference/evidence-lane matrices, and close DEF-021 by adding collision-bearing presentation geometry instead of moving the authored lens anchor or changing symbolic state | user requested K-Dense-AI-inspired contribution/reference/topic cross-validation plus higher game completeness; audit found the 45-reference topic map and experiment lanes implicit, while the beacon-directed lens approach repeatedly crossed the quay edge | CI now binds 5 contributions, 45 bibliography keys, 9 topics, E1–E3 plus ENG1, with every `C-RESULT-*` still prohibited; `LensApproachDeck` contains `(-11,1,1)`, overlaps the quay, wraps the store wall, and is proved inside the stable 8/8 smoke item; terminal hash and canonical `project.godot` hash stay unchanged; deployment remains approval-gated | Keep |
| D-056 | 2026-08-30 | Run the engine-bearing canonical-fixture test only from the shared cache-free disposable Godot staging helper, never by importing or executing `game-track/godot` directly | the CI-equivalent gate unexpectedly rewrote indentation in the tracked balance receipt even though `project.godot` stayed stable; isolation proved that the test's direct canonical `--import` caused the mutation | `test_all_headless_fixtures_when_godot_4_is_available` now stages once, imports the copy, and runs all four fixtures there; a post-fix 48-test validation preserved SHA-256 for both canonical `project.godot` and `balance-archetypes.json`; DEF-023 closed | Keep |
| D-057 | 2026-08-30 | Close the contribution map in both directions and make the actual eight-item 3D smoke part of the Godot-available disposable-copy runtime test, with collider structure derived from the single-owned lens anchor | final review found one reverse-only C1 reference, one undeclared C3 topic, an unenforced ENG1 manuscript marker, and a smoke check that did not inspect the collision shape or run through the aggregate local test path | C1 now includes S20, C3 includes T4, the validator enforces forward/reverse reference-topic symmetry, all 14 live receipt hashes/bytes and seven five-seed K=1 pairs, and bilingual ENG1 markers; the smoke now requires an enabled matching `BoxShape3D` and runs after all four fixture checks without mutating canonical sources | Keep |

| D-058 | 2026-08-30 | Deploy the approved contribution-first and lens-route build from commit `5d06e3b`, then synchronize one factual production receipt without upgrading any research or human-impact gate | user explicitly approved the `5d06e3b` push, Vercel production deployment, and one receipt follow-up commit | deployment `dpl_Auzz4gjVUcgDcL45EjcRG2HVyoCW` is READY at `https://sealed-lighthouse-trace-rpg.vercel.app`; 11 manifest files / 50,746,755 bytes, 10 public runtime files / 50,746,242 bytes, PCK 10,893,980 bytes / `654c1f13…a58e7`; all runtime files returned anonymous 200 and were byte-identical; WASM/OFL MIME and configured headers matched, `vercel.json` returned 404, and desktop start gate → three-page tutorial → in-game logged zero console/page errors; production save/reload, current mobile, pointer/audio, warmed performance, soak, rollback, and human-impact evidence remain open | Keep |

| D-059 | 2026-08-30 | Adopt one bilingual editable-visual contract for every reported table and figure: generator plus SVG/LaTeX/data as source, same-stem source-linked vector PDF for paper figures, and trace-bound replay sources as the only non-editable engine-evidence exception | user directive to remove line/text overlap, make comparisons legible, retain editable originals, and keep the rule for future assets | `visuals/source-manifest.json` binds 13 SVG assets, 5 table-source families, 2 paper surfaces, and 1 engine-evidence exception through 88 unique receipts; source geometry, label shields, accessibility metadata, booktabs/row spacing, matched deltas, mixed-denominator scope notes, Type 3/raster exclusions, and isolated double-regeneration of 20 lightweight outputs are CI-gated; EN/KO PDFs remain eight pages, canonical `project.godot`, balance JSON, terminal hash, and every `C-RESULT-*` evidence ceiling remain unchanged; commit/push requires separate approval | Keep |
| D-060 | 2026-09-02 | Make the manuscript's technique and evidence legible at a glance and archival-first in citation: generate a contribution/topic/lane/ceiling table and an evidence-lane design table from the machine-checked matrices, link every committed guided-repair case to its Table-rho edit by validator code, add five top-venue lineage references (ASPLOS 2006 CEGIS, NeurIPS 2023 Reflexion, ACM CSUR 1983 transactions, ICML 2024 LLM-Modulo, CoRL 2022 SayCan) and resolve Self-Refine to its NeurIPS 2023 DOI, replicate the frozen Stage-4 packet in an isolated directory, and mirror the paper's predicate families diegetically in the playable ledger (`[V] GATE <family> | state unchanged`, end-card `HOLDS BY GATE`) | user directive for five improvement loops in which the core technique is visible, experiments are explicit, contributions are clear, and major-venue references are prioritised; Aside deep research over Crossref/OpenAlex/PMLR (Semantic Scholar 429 throughout, DBLP unreachable, OpenReview blocked so Self-Debug stays PREPRINT) | bibliography is 50 = VERIFIED 45 + PREPRINT 5 with crosswalk, Stage-5 JSON/MD, README, and validator constants moved together; generated `contribution_map_*.tex` and `evidence_lanes_*.tex` are registered as source-manifest table families with the two CSVs staged for double regeneration; EN/KO PDFs remain eight pages with zero overfull boxes after prose compression; `stage-04-replication-2026-09-02.md` records byte-identical CSVs and identical `pilot-results.json`; disposable-copy 3D smoke `8/8` with terminal SHA-256 `4b231017…108892`, `SL-PLAY-EVAL-001` `4/4`/`49/49` with four refreshed working captures re-registered in `qa/gate-measurements.md`, canonical `project.godot` hash unchanged; Web artifact rebuilt locally (PCK 10,895,004 bytes, SHA-256 `d323af364d18…019a7e`) but not deployed, so the production alias still serves `dpl_Auzz4gjVUcgDcL45EjcRG2HVyoCW`; no `C-RESULT-*` claim, G4, or G6 status changes | Keep |
| D-062 | 2026-09-02 | Make fun and player contribution legible as a presentation-only readout of committed snapshots (`CASE CHAIN` HUD line, `CONTRIBUTION #N` + `UNLOCKED` after every commit, `RULE LEARNED`/`RULE RECALLED` after every hold, two-part end-card receipt), extend `SL-PLAY-EVAL-001` to 12 presentation invariants (52/52), freeze the fun/contribution design hypothesis `SL-HYP-001` and GDD §11 targets B-014–B-017, and recompose the manuscript (≤200-word abstract, bold thesis, one-line C1–C5 with section/lane pointers, single-column Fig. 1 transaction pipeline + ledger-grammar band on page 2, Godot captures as a numbered causal sequence with bound state fields, legacy `fig_repair_state_machine` and `fig_evidence_boundary` diagrams removed as redundant with Fig. 1 and Table IV, five verified 2025–2026 references S52–S56) | user directive to improve game UI/types/design/systems from search-grounded Aside feedback so that fun and contribution stand out, then re-review the paper and improve its page composition; the two Aside reports are filed as intake (`[INFERENCE]`, not evidence); D-061 is reserved by the concurrent lock-holding Aside session `BOZW50yOGZJCIJso` (scope `d061-independent-review-and-strengthening`), so this pass takes D-062 and appends after it by agreement | `contribution_delta` is a pure diff of the two snapshots the hard writer returns; no canonical mutation, save, or schema change; smoke `8/8` and terminal hash `4b2310…8892` unchanged; `run_playable_evaluation.py` → fixtures 4/4, checks 52/52, screenshots 4/4; disposable Web stage rendered the new lines at 1280×720 and 390×844 with zero console/page errors; four working captures refreshed and hash-registered; design-hypothesis, game-ui, and studio validators PASS; crosswalk 55 references PASS; EN 8 pp / KO 8 pp, Overfull 0, Type 3 fonts 0; player effect on competence, agency, or frustration remains unmeasured (`[TARGET]`, G4 FIX); the lock holder's non-idempotent `ps2pdf -dDeterministicID` figure step and in-flight validator/figure edits are recorded in `conflicts.md` | Keep |

| D-061 | 2026-09-02 | Independently audit and minimally harden the contribution-first paper/game packet: make the seven deterministic validator checks a code-owned `VALIDATOR_CHECKS` contract grouped into six state-relative families, fail closed on missing or stale editable-visual receipts, make SVG/PDF regeneration deterministic, correct bibliography and rate-limit accounting to 55 entries (46 `VERIFIED`, 9 `PREPRINT`, 22 Semantic Scholar lookups rate-limited), remove real label collisions without shrinking below the type floor, and replace the concurrent D-062 playtest packet's invented sample/percentage thresholds with a study-owner rule frozen before recruitment | user request to review and strengthen; eight independent review lanes, direct generator/validator inspection, isolated double-regeneration, PDF text/font/page inspection, browser SVG geometry measurement, and post-integration game-design/UI/feel contract validation; D-061 was reserved before the concurrent D-062 pass and is appended afterward to preserve both writers' append-only history | all nine Godot rejection codes map to a named gate; crosswalk validation binds 5 contributions, 55 references, 9 topics, E1–E3 plus ENG1 and the 7-check/6-family wording; visual validation covers 11 SVG assets, 22 lightweight generated outputs, and 88 unique source/artifact receipts; EN/KO remain 8 pages with zero overfull boxes and zero Type 3 fonts; a hold-free golden path may correctly report 0 learned gates; human competence, agency, frustration, accessibility, G4, and G6 remain unmeasured; no commit, push, or deployment is authorized by this review | Keep |
| D-063 | 2026-09-02 | Treat `FACT_LABEL` as a fail-closed player-visible allowlist: unknown or future canonical fact IDs stay internal and the contribution UI falls back to generic `State recorded` copy rather than rendering a raw identifier | D-061 contract-to-code review found that the D-062 raw-ID fallback contradicted the UI contract's sealed-label boundary and could expose a future private fact after a valid commit | `player_visible_fact_labels()` now filters labels before ledger/end-card rendering; the existing `contribution_delta_is_pure_and_names_facts` evaluation also probes one public, one sealed, and one unknown fact and emits only `Signal lens secured`; a static regression guards all three oracle labels; staged Godot remains 52/52 with the canonical project and terminal state unchanged; this is engineering privacy conformance, not player-impact evidence | Keep |
| D-064 | 2026-09-02 | Record the playable's golden-path route with Godot Movie Maker through a dev-only `--autoplay` autopilot instead of a browser screen capture, publish the derived GIF/MP4 as the README play video, fact-check every bibliography link, and rewrite both READMEs to the current state | user directive to fact-check reference links with Aside, record a play video as a README GIF, refresh the README to current state, and push | `--autoplay` substitutes only for input and drives the existing interaction/choice handlers (3 commits, 1 hold, terminal SHA-256 `4b231017…108892`, smoke `8/8`, `SL-PLAY-EVAL-001` `52/52`, canonical `project.godot` unchanged); `trace-rpg-gameplay.mp4` (63.433 s, 1,903 frames) and `golden-path.gif` (560×315, 4,675,182 B) now record the current local build rather than the predecessor deployment; `stage-05-link-audit-2026-09-02.md` records 55/55 resolving links with 37 Crossref title matches, 17 landing-page title matches, and S53 human-verifiable only; the bilingual READMEs lead with the play video and the evidence map; the Vercel alias still serves `dpl_Auzz4gjVUcgDcL45EjcRG2HVyoCW` and no `C-RESULT-*`, G4, or G6 status changes | Keep |
| D-065 | 2026-09-02 | Add an AgentSight-style live dashboard that embeds the public-safe Web build and visualizes the paper's commit gate while it is played: a presentation-only `postMessage` mirror in `game_3d.gd` (typed events, committed-snapshot hashes, codes, predicate families) feeds a weighted pipeline graph, a `top`-style metrics panel with the trace hash chain and frozen E1/E2 counts, an event timeline, and a harbor mini-map | user directive to visualize the paper's content as a dashboard that runs the gameplay and shows metric linkage in real time, built collaboratively through the Aside browser; AgentSight `vis`/`top` idioms (effect-weighted nodes, animated replay, live session table) adopted | bridge is presentation-only and one-directional, enabled only when embedded; sealed fact IDs never leave the game; Aside-driven route inside the dashboard produced 3 commits, 1 hold (`FORBIDDEN_DISCLOSURE` → DISCLOSURE), hash chain `f488d9c4…812c → 19b474dc…c498 → 93381457…b900 → 4b231017…8892` with the final hash equal to the headless smoke terminal hash; smoke `8/8`, `SL-PLAY-EVAL-001` `52/52`, canonical `project.godot` unchanged; the dashboard's live counts are engineering demonstration only and promote no claim, G4, or G6 status | Keep |
| D-066 | 2026-09-03 | Show the gate as the player reads it inside the manuscripts and republish the entry documentation image-first: add Fig.~3 `fig:playable-readout` (held ledger, committed contribution clause, end-card receipt) to both EN/KO manuscripts as LaTeX-trim crops of the tracked working captures bound as a second non-editable evidence exception, rewrite both manuscripts as a presentation-flow narrative without agent-workflow tokens (claim/ledger IDs, matrix file names, git tags, manifest counts, CLI names), cross-verify all 55 bibliography entries (three primary-source agents, then three Aside browser-research sessions weighted higher per user instruction; corrections applied for S01, S06, S12, S19, S25, S46, S53 and four citation sentences narrowed), and replace the removed READMEs with a root `README.md` (English default) plus `README.ko.md` (switch link) carrying badges, the play/dashboard GIFs, paper page sheets, and the 16 Korean deck slides | user directive 2026-09-03 (발표자료 캡처를 논문에, 배지·이미지 중심 리드미 EN 기본/KO 전환, 발표 흐름 위주 논문 정리, 레퍼런스 팩트체크, 깃 푸시); three primary-source agent checks (55 KEEP) followed by three Aside browser cross-checks (55 resolvable; 2 archival promotions, 5 metadata fixes, 4 wording narrowings) | the figure shows engineering captures as a presentation-only readout and adds no usability, efficacy, G4, or G6 status; the crosswalk validator now requires the machine-checked-matrices phrase instead of CSV file names in the availability paragraph; bibliography notes and the citation audit carry the 2026-09-03 cross-verification (48 VERIFIED / 7 PREPRINT / 22 rate-limited; validator totals updated); no `C-RESULT-*` status changes | Keep |
| D-067 | 2026-09-03 | Separate the manuscripts' Fig. 3 from promoted evidence: freeze the three working captures it crops as a dated set `paper/latex/captures/playable-readout-20260903/` (PNG + `<stage>.shot.json` + `receipt.json` with trim boxes), bind it in the visual manifest as an `engine-working-capture` exception that is illustration only, make `run_playable_evaluation.py --capture` write and merge `SHOT-SAVED` receipts (capture method, canonical state hash before/after each stage) so `docs/latest` captures carry a real stage→state binding, refresh `docs/latest` through that native path, restore Korean inter-word spacing in the KO manuscript (`\xeCJKsetup{CJKspace=true}`), name the curated generated UI art in both AI-usage disclosures, and describe the live proposer tool-neutrally | independent review of D-066 found the figure pointed at refreshable Web-stage captures whose manifest sources could not regenerate them, the KO PDF had shipped with every Hangul inter-word space collapsed, and the disclosures omitted the generated imagery embedded in the figure | Fig. 3 and its caption/ENG1 text are illustration only; ENG1 conformance claims rest on the replayed trace and deterministic evaluation; `docs/latest` pins updated in `qa/gate-measurements.md` and `qa/browser-qa.md`; no `C-RESULT-*`, G4, or G6 status changes | Keep |
| D-068 | 2026-09-03 | Serve the live commit-gate dashboard as the production site: deploy through `scripts/deploy_vercel_dashboard.sh` with the local run-guide layout (`/dashboard/` + `/public/`), a root redirect to `/dashboard/`, an `/index.html` → `/public/index.html` redirect for old links, and `outputDirectory: dist` in `game-track/web/vercel.dashboard.json` so Vercel's `public/`-as-root default cannot swallow the dashboard; add a favicon link and a three-column KPI wrap under 720 px | user directive "vercel 배포는 대시보드버전으로 배포해줘"; first attempt served only `public/` (Vercel Other preset), diagnosed with `vercel project inspect` | production `dpl_G58qo7siKGUP2E55RH2Xxj8pPgtF` READY at `https://sealed-lighthouse-trace-rpg.vercel.app`; 10 runtime + 4 dashboard files byte-identical; headless desktop/mobile checks with zero console/page errors; presentation-only, one-directional bridge; no research, G4, or G6 status change | Keep |
