# Cycle 1 retrospective — bounded authored-fixture approval

Run ID: `20260813-sealed-lighthouse-cycle-1`  
Closed: 2026-08-13  
Director decision: bounded approval under `D-014`  
Next-cycle entry: Stage 1 presentation/evidence hardening

## Outcome

Cycle 1 established the bilingual experimental-game design, a Godot 4.7.1 headless policy-mirror
slice, immutable evidence-set promotion, deterministic rejection/save/load/replay fixtures, stable
bridge-envelope projection tests, and a frozen concept pack. The independent integrator and logic
auditor accepted that packet only within the authored-fixture boundary.

## Gate close table

| Gate | Measured evidence at close | Verdict | Carry-forward risk |
|---|---|---|---|
| G1 | 0 unwaived conflicts in enumerated fixture/concept audit; no complete playable export | `FIX` | audit all future player-visible render strings |
| G2 | no balance simulation | `NOT_SCHEDULED_STAGE_1` | mechanics and matchup bands unmeasured |
| G3 | 0 executed archetypes | `NOT_SCHEDULED_STAGE_1` | diversity and dominance unmeasured |
| G4 | no participant data or scored scenes | `NOT_SCHEDULED_NO_HUMAN_DATA` | immersion/readability remain unmeasured |
| G5 | zero-economy scope under `D-006` | `SCOPED_OVERRIDE_NOT_EMPIRICAL_PASS` | no paid/free result exists |
| G6 | three retained runs matched terminal/oracle hashes; startup-heavy frame p95 `116.667/95.390/100.000 ms`; no soak/input evidence | `FIX` | performance and live transport remain open |
| G7 | three valid commits plus guarded failures in each retained authored run; no human loop duration/re-entry | `FIX` | repeat proxy unmeasured |
| G8 | two bounded `0/7` advertised-frequency candidates; no human impression score | `FIX` | novelty effect unmeasured |

Numeric and method details remain authoritative in `../qa/gate-measurements.md`; this retrospective
does not upgrade them.

## What changed the next cycle

- The headless path was sufficient for deterministic conformance but could not expose a paper-ready
  engine-render/state correspondence surface.
- Immutable promotion and explicit claim boundaries worked: faults stayed visible instead of being
  deleted or relabelled as success.
- Generated concept art remained useful for authoring, but its separation from the primary
  structured-state experiment needs to remain mechanically obvious.

Cycle 2 therefore re-enters Stage 1 for one bounded, separate non-headless render-capture pass. It
must retain the headless trace as the conformance authority, bind each PNG to its exact authored
source beat, exclude generated art from the primary panels, and preserve all G4/G6/efficacy
blockers. After independent verification, the studio returns to the live Python authorization and
performance-evidence beat.
